import { SpacingLeftDirective } from './spacing-left.directive';

describe('SpacingLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new SpacingLeftDirective();
    expect(directive).toBeTruthy();
  });
});
